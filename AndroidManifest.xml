<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Добавьте это разрешение для Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacroRecorder"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34"> <!-- Измените на 34 -->

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MacroRecorder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity со списком пресетов -->
        <activity
            android:name=".PresetListActivity"
            android:exported="false"
            android:theme="@style/Theme.MacroRecorder.NoActionBar" />

        <!-- Сервис плавающей кнопки -->
        <service
            android:name=".MainService"
            android:exported="false"
            android:foregroundServiceType="shortService"
            android:enabled="true"
            android:permission="android.permission.FOREGROUND_SERVICE" />

        <!-- Accessibility Service -->
        <service
            android:name=".MacroAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:enabled="true"
            android:process=":accessibility_process">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Будьте осторожны с этим тегом, если у вас нет файла backup_rules -->
        <!-- Если файла нет, удалите следующую строку: -->
        <!-- android:fullBackupContent="@xml/backup_rules" -->

    </application>

</manifest>